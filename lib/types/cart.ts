// Re-exports from the canonical schema file.
// Import from here OR from @/lib/schemas/cart — both resolve to the same type.
export type {
  AddToCartInput,
  CartFieldErrors,
  CartActionState,
} from '@/lib/schemas/cart'
